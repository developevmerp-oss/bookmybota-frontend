"use client";

import PartnerPromotionsPage from "@/components/Shared/PartnerPromotionsPage";

export default function OrganizerPromotionsPage() {
  return (
    <PartnerPromotionsPage
      module="EVENTS"
      title="Marketing Promotions"
      description="Promote your events on the landing slider, event listings, and event detail pages."
    />
  );
}
