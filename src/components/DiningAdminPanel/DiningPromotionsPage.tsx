"use client";

import PartnerPromotionsPage from "@/components/Shared/PartnerPromotionsPage";

export default function DiningPromotionsPage() {
  return (
    <PartnerPromotionsPage
      module="DINING"
      title="Marketing Promotions"
      description="Request visibility promotions by plan. After Super Admin approval, your restaurant appears on the landing slider and dining listings."
    />
  );
}
